import React from 'react';
import styles from './SNB.module.scss';

const SNB = () => {
  return (
    <aside className={styles.aside}>
      <nav className={styles.nav}>SNB</nav>
    </aside>
  );
};

export default SNB;
